version: 2
jobs:
  build:
    docker:
      - image: cimg/go:1.14
    working_directory: ~/build
    steps:
      - checkout
      - run:
          name: Build
          command: |
            make lint
            make test
